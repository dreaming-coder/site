<template>
  <table>
    <caption v-if="caption">{{ caption }}</caption>
    <thead>
    <tr>
      <th class="head">属性</th>
      <th class="head">描述</th>
    </tr>
    </thead>
    <tbody>
      <tr  v-for="(item,index) of items" :key="index">
        <td class="column-1">{{item.field}}</td>
        <td>{{item.miaoshu}}</td>
      </tr>
    </tbody>
  </table>
</template>

<script>
export default {
  name: "Mybatis-Association-ResultSet",
  data(){
    return {
      caption: "",
      items: [
        {field:"column", miaoshu: "当使用多个结果集时，该属性指定结果集中用于与 foreignColumn 匹配的列（多个列名以逗号隔开），以识别关系中的父类型与子类型。"},
        {field:"foreignColumn", miaoshu: "指定外键对应的列名，指定的列将与父类型中 column 的给出的列进行匹配。"},
        {field:"resultSet", miaoshu: "指定用于加载复杂类型的结果集名字。"},
      ]
    }
  }

}
</script>

<style scoped>
caption {
  font-size: 1.2em;
  font-weight: bolder;
  margin-bottom: 0.5em;
}
.column-1 {
  text-align: center;
  white-space:nowrap;
  font-weight: bold;
}
td{
  height: 2em;
}
</style>